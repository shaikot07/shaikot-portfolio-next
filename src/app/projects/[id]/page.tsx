import React from 'react';

const ProjectDetailsPage = () => {
    return (
        <div>
            this is the project details page with dynamic routing
        </div>
    );
};

export default ProjectDetailsPage;